[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm ci",
  "npm ci --prefix ../client --legacy-peer-deps"
]

[phases.build]
cmds = [
  "npm run prisma:generate",
  "npm run build --prefix ../client"
]
