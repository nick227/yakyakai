{
  "name": "yakyakai-poc",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client && npm run prisma:generate --prefix server",
    "start": "npm run dev",
    "start:prod": "concurrently \"npm run start --prefix server\" \"npm run worker --prefix server\"",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "db:push": "npm run db:push --prefix server",
    "db:migrate": "npm run db:migrate --prefix server",
    "db:deploy": "npm run db:deploy --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
